# Build/packaging dependencies (not needed to just run the app)
# Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pyinstaller>=6.6,<7
pillow>=10,<13  # scripts/make_wizard_images.py
